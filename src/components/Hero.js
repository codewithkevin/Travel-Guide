import React from 'react';
import Hero1 from './images/Hero.png';

const Hero = () => {
    return (
        <div className="hero">
            <img src={Hero1} alt="hero" />
        </div>
    );
};

export default Hero;

